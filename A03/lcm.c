#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
unsigned int LCM(unsigned int a, unsigned int b)
{
    unsigned int n;
    if (a == 0 || b == 0)
        return (0);
    if (a > b)
        n = a;
    else
        n = b;
    while(1)
    {
        
    }
}